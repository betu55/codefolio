spring.application.name=codefolio
spring.config.import=optional:file:.env[.properties]

# Database configuration for PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/codefolio
spring.datasource.username=betu
spring.datasource.password=secret123
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

#jwt
jwt.secret=KuG4te1mZGF6VdDz9abZwaMjl58w+klU2d4xqVLCGOg=
jwt.expiration=86400000

# Optional admin bootstrap for local development.
app.admin.name=${ADMIN_NAME:Portfolio Admin}
app.admin.email=${ADMIN_EMAIL:}
app.admin.password=${ADMIN_PASSWORD:}

#server
server.port=8081

# Database configuration for H2 (in-memory database)
# spring.datasource.url=jdbc:h2:mem:testdb
# spring.datasource.driverClassName=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=

# JPA/Hibernate properties
# spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
# spring.jpa.hibernate.ddl-auto=create-drop
# spring.jpa.show-sql=true

# H2 Console (useful for debugging)
# spring.h2.console.enabled=true
